# ...existing content...

## API Endpoints

- Listings: `/api/listings/`
- Bookings: `/api/bookings/`

Supports GET, POST, PUT, DELETE for both endpoints.

## Swagger Documentation

Swagger UI is available at `/swagger/` (if drf-yasg or similar is installed).

## Testing

Use Postman or similar tools to test all endpoints.

# ...existing content...